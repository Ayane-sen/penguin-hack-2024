export type User = {
  id: string;
  name: string;
};

export type NextClassInfo = {
  nextTopic: string;
  nextTest: string;
};

export type ClassListItem = {
  title: string;
  at: string;
  comprehension: number;
};
